{
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "ts-node-dev --respawn --inspect=0.0.0.0 --transpile-only index.ts"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "fastify": "^3.18.0",
    "fastify-cors": "^6.0.1"
  },
  "devDependencies": {
    "@types/node": "^15.12.4",
    "ts-node-dev": "^1.1.7",
    "typescript": "^4.3.4"
  }
}
